version: 0.0.2
scripts:
  # Script executed when the container is first loaded
  # eg: bootstrap: pip install pandas
  bootstrap: |
    npm install
    pip install -r requirements.txt
dependencies:
  pdf-craft: 0.0.6
name: pdf-craft-starter
displayName: PDF Craft Starter
description: Starter project using the pdf-craft package.
releaseType: project
repository: https://github.com/oomol-flows/pdf-craft-starter
releaseNotes: Init project
icon: ./icon.png
