version: 0.1.0
scripts:
  # Script executed when the container is first loaded
  # eg: bootstrap: pip install pandas
  bootstrap: |
    npm install
    pip install -r requirements.txt
dependencies:
  pdf-craft-cloud: 0.1.2
  oomol-preview: 0.0.3
name: pdf-craft-cloud-starter
displayName: PDF Craft Cloud Starter
description: OOMOL Starter project using the pdf-craft package.
releaseType: project
repository: https://github.com/oomol-flows/pdf-craft-starter
releaseNotes: support LaTeX & table recognization.
icon: ./icon.png
